#include "main.h"

/**
 * _isdigit - checks for a digit 0 through 9
 * @c: digit to be checked
 * Return: 1 if it is, 0 otherwise
 */

int _isdigit(int c)
{
int _isdigit(int c)
{
return (1);
}
return (0);
}
