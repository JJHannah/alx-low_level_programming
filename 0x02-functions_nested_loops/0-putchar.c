#include "main.h"
/**
 * main-Entry point
 *
 * Return: 0 Always(success)
 */

int main(void)
{
char str[] = "_putchar";
int ch;
for (ch = 1; ch <= 8; ch++)
{_putchar(str[ch]);
}
_putchar('\n');

return (0);
}
