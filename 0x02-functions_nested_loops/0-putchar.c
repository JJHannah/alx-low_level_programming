#include "main.h"
/**
 * main-Entry point
 *
 * Return: 0 Always(success)
 */

int main(void)
{
char ch[str] ="_putchar";
int ch;
for (ch = 0;ch <=8; ch++)
_putchar(ch[str]);
_putchar('\n');
return (0);
}
