#include "main.h"
#include <stdio.h>

/**
 *cap_string - capitalize first letter of each word
 *@s: string to manipulate
 *
 *Return: string
 */

char *cap_string(char *s)
{
int j = 0;

if (s[j] >= 'a' && s[j] <= 'z')



